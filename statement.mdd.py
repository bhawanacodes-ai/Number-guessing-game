Problem Statement

Develop a Python program to create an interactive Number Guessing Game in which

the computer randomly generates a number within a specified range, and the user

attempts to guess the generated number.

The program should ask the user to enter a guess and compare it with the randomly generated number.
If the user's guess is greater than the generated number, the program should display a message
indicating that the guess is too high. If the guess is smaller, the program should
indicate that the guess is too low. If the user guesses the correct number,
the program should display a congratulatory message.

The program should continue accepting guesses until the user correctly identifies
the number. It should also keep track of the number of attempts made by the user and
display the total number of attempts after the correct guess.

The objective of this program is to demonstrate the use of random number generation,
variables, conditional statements, loops, user input, and basic Python programming concepts.
